name: Video Edit

on:
  workflow_dispatch:
    inputs:
      static_text:
        description: "Static text for first phase (0-2s)"
        required: false
        default: "12 فرزند"
      marquee_text:
        description: "Marquee text (2s-end)"
        required: false
        default: "برای خرید فالوور لایک ویدیو اینستاگرام به آیدی @insta_shop پیام بدید"
      watermark_text:
        description: "Watermark text"
        required: false
        default: "@insta_shop"

env:
  # Force UTF-8 everywhere. Persian/Arabic text otherwise gets corrupted to
  # "?" by ASCII decoding under a POSIX locale, producing "????" in the video.
  PYTHONUTF8: "1"
  PYTHONIOENCODING: "utf-8"
  LANG: "C.UTF-8"
  LC_ALL: "C.UTF-8"

jobs:
  edit:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install FFmpeg
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: "pip"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install Pillow arabic-reshaper python-bidi requests

    - name: Set up fonts
      run: |
        mkdir -p fonts
        if [ -f "fonts/Vazirmatn-Bold.ttf" ]; then
          echo "Using bundled Vazirmatn font"
          ls -la fonts/
        else
          echo "Downloading Vazirmatn font..."
          wget -q -O vazirmatn.zip "https://github.com/rastikerdar/vazirmatn/releases/download/v33.003/vazirmatn-v33.003.zip"
          unzip -jo vazirmatn.zip "fonts/ttf/Vazirmatn-Bold.ttf" -d fonts/
          rm -f vazirmatn.zip
          echo "Downloaded Vazirmatn-Bold.ttf"
        fi
        ls -la fonts/
        file fonts/Vazirmatn-Bold.ttf

    - name: Verify Persian text arrives intact (guard against mojibake)
      run: |
        python - <<'EOF'
        import sys
        for name in ("STATIC_TEXT", "MARQUEE_TEXT", "WATERMARK_TEXT"):
            val = __import__("os").environ.get(name, "")
            ok = any("\u0600" <= c <= "\u06FF" or "\u06F0" <= c <= "\u06F9"
                     for c in val) if val else True
            print(f"{name}: {val!r} has_persian={ok}")
            # If Persian was expected but arrived as '?' the dispatch encoding broke.
            if val and any(c in val for c in ("?", "\ufffd")):
                print(f"ERROR: {name} is corrupted (contains '?'). "
                      "The workflow_dispatch request was not UTF-8 encoded.", file=sys.stderr)
                sys.exit(1)
        EOF

    - name: Edit video
      env:
        STATIC_TEXT: ${{ github.event.inputs.static_text }}
        MARQUEE_TEXT: ${{ github.event.inputs.marquee_text }}
        WATERMARK_TEXT: ${{ github.event.inputs.watermark_text }}
      run: |
        python main.py video.mp4 output.mp4 \
          --template default \
          --static-text "$STATIC_TEXT" \
          --marquee-text "$MARQUEE_TEXT" \
          --watermark-text "$WATERMARK_TEXT"

    - name: Upload output
      uses: actions/upload-artifact@v4
      with:
        name: edited-video
        path: outputs/output.mp4
        retention-days: 7
