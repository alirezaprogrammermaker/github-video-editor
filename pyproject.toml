[project]
name = "github-video-editor"
version = "1.0.0"
description = "Persian/Arabic text overlay video editor powered by FFmpeg"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[project.scripts]
video-editor = "main:main"
